# Generated by XcodeGen — project.yml is the source of truth.
Offhook.xcodeproj/

.DS_Store

# Xcode / SPM build products
.build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/
build/

# Secrets — credentials never enter this repo (canonical store: ../secrets/, outside the tree;
# tests read env vars first, that file second). Defense in depth against accidental drops.
*.env
Secrets/
secrets/
